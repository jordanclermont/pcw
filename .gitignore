# OS cruft
.DS_Store
Thumbs.db

# Claude Code per-user local config (permissions etc.) — not shared
.claude/settings.local.json

# Logs / temp
*.log

# WWF.com reference screenshots — local design refs, not shipped (third-party imagery)
references/
